quarkus.application.name=reporting-service
quarkus.application.version=1.0-SNAPSHOT

quarkus.http.host=0.0.0.0
quarkus.http.port=${HTTP_PORT:8080}
%dev.quarkus.http.port=8087
%test.quarkus.http.test-port=0

# High-concurrency guardrails: bounded requests prevent slow clients and large bodies
# from occupying worker threads indefinitely. Tune per instance, not globally by habit.
quarkus.http.idle-timeout=${HTTP_IDLE_TIMEOUT:30S}
quarkus.http.limits.max-body-size=${HTTP_MAX_BODY_SIZE:2M}
quarkus.shutdown.timeout=${GRACEFUL_SHUTDOWN_TIMEOUT:30S}
# JDBC and report rendering are blocking; keep workers finite so pressure is visible
# before DB connections or CPU-heavy exports are overwhelmed.
quarkus.thread-pool.max-threads=${APP_MAX_WORKER_THREADS:32}

quarkus.datasource.db-kind=${DB_KIND:postgresql}
quarkus.datasource.username=${DB_USERNAME:reporting-service}
quarkus.datasource.password=${DB_PASSWORD:npg_3zyqDGfAWI2N}
%prod.quarkus.datasource.password=${DB_PASSWORD:npg_3zyqDGfAWI2N}
quarkus.datasource.jdbc.url=${DB_JDBC_URL:jdbc:postgresql://ep-flat-meadow-ap1zpfr6-pooler.c-7.us-east-1.aws.neon.tech/reporting-service-database?user=reporting-service&password=npg_3zyqDGfAWI2N&sslmode=require&channelBinding=require}
# Pool defaults are intentionally modest. Raise DB_POOL_MAX_SIZE only after checking
# datasource wait time, DB CPU, lock waits and p95 latency; prefer horizontal scale first.
quarkus.datasource.jdbc.min-size=${DB_POOL_MIN_SIZE:2}
quarkus.datasource.jdbc.max-size=${DB_POOL_MAX_SIZE:16}
quarkus.datasource.jdbc.acquisition-timeout=${DB_ACQUISITION_TIMEOUT:5S}
quarkus.datasource.jdbc.background-validation-interval=${DB_BACKGROUND_VALIDATION_INTERVAL:2M}
quarkus.datasource.jdbc.idle-removal-interval=${DB_IDLE_REMOVAL_INTERVAL:5M}
quarkus.datasource.jdbc.max-lifetime=${DB_MAX_LIFETIME:30M}
quarkus.datasource.health.enabled=true
quarkus.datasource.metrics.enabled=true
quarkus.flyway.migrate-at-start=${FLYWAY_MIGRATE_AT_START:true}
quarkus.flyway.validate-on-migrate=true
quarkus.flyway.clean-disabled=true
quarkus.flyway.locations=db/migration
%test.quarkus.datasource.db-kind=h2
%test.quarkus.datasource.username=sa
%test.quarkus.datasource.password=sa
%test.quarkus.datasource.jdbc.url=jdbc:h2:mem:reporting_service;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
%test.quarkus.datasource.jdbc.min-size=1
%test.quarkus.datasource.jdbc.max-size=4
%test.quarkus.datasource.devservices.enabled=false

reporting.internal-token=${INTERNAL_SERVICE_TOKEN:dev-internal-token-change-me}
%prod.reporting.internal-token=${INTERNAL_SERVICE_TOKEN}
reporting.jwt.issuer=${AUTH_JWT_ISSUER:brasaller-auth}
reporting.jwt.audience=${AUTH_JWT_AUDIENCE:brasaller-platform}
reporting.jwt.secret=${AUTH_JWT_SECRET:dev-only-change-me-please-32-bytes-minimum}
%prod.reporting.jwt.secret=${AUTH_JWT_SECRET}
reporting.cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:not-configured}
reporting.cloudinary.api-key=${CLOUDINARY_API_KEY:not-configured}
reporting.cloudinary.api-secret=${CLOUDINARY_API_SECRET:not-configured}
reporting.cloudinary.expense-folder=${CLOUDINARY_EXPENSE_FOLDER:brasaller/despesas}
reporting.cloudinary.resource-type=${CLOUDINARY_RESOURCE_TYPE:auto}
reporting.clicksign.base-url=${CLICKSIGN_BASE_URL:https://sandbox.clicksign.com/api/v3}
reporting.clicksign.access-token=${CLICKSIGN_ACCESS_TOKEN:not-configured}
%prod.reporting.clicksign.access-token=${CLICKSIGN_ACCESS_TOKEN}
reporting.clicksign.webhook-secret=${CLICKSIGN_WEBHOOK_SECRET:not-configured}
%prod.reporting.clicksign.webhook-secret=${CLICKSIGN_WEBHOOK_SECRET}
reporting.clicksign.default-deadline-days=${CLICKSIGN_DEFAULT_DEADLINE_DAYS:7}
%test.reporting.cloudinary.cloud-name=brasaller-test
%test.reporting.cloudinary.api-key=test-api-key
%test.reporting.cloudinary.api-secret=test-api-secret
%test.reporting.cloudinary.expense-folder=brasaller/despesas
%test.reporting.cloudinary.resource-type=auto
%test.reporting.clicksign.webhook-secret=test-webhook-secret

messaging.outbox.dispatch-every=${MESSAGING_OUTBOX_DISPATCH_EVERY:5s}
messaging.outbox.batch-size=${MESSAGING_OUTBOX_BATCH_SIZE:50}
messaging.outbox.max-attempts=${MESSAGING_OUTBOX_MAX_ATTEMPTS:10}
messaging.outbox.retry-delay-seconds=${MESSAGING_OUTBOX_RETRY_DELAY_SECONDS:30}
messaging.outbox.in-flight-timeout-seconds=${MESSAGING_OUTBOX_IN_FLIGHT_TIMEOUT_SECONDS:300}

quarkus.http.cors.enabled=true
quarkus.http.cors.origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:4200,http://localhost:8080,https://aistudio.google.com}
quarkus.http.cors.methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
quarkus.http.cors.headers=accept,authorization,content-type,x-request-id,x-tenant-id
quarkus.http.cors.exposed-headers=location,x-request-id,content-disposition,x-report-filename

quarkus.smallrye-openapi.path=/q/openapi
mp.openapi.filter=com.example.interfaces.rest.OpenApiSecurityFilter
quarkus.smallrye-openapi.info-title=BraSeller Reporting Service API
quarkus.smallrye-openapi.info-version=1.0.0
quarkus.smallrye-openapi.info-description=API de painel financeiro, relatorios e graficos multi-marketplace do BraSeller.
quarkus.swagger-ui.always-include=${SWAGGER_UI_ENABLED:false}
%dev.quarkus.swagger-ui.always-include=true
%test.quarkus.swagger-ui.always-include=true

quarkus.micrometer.export.prometheus.path=/q/metrics
quarkus.micrometer.binder.http-server.enabled=true

quarkus.log.level=${LOG_LEVEL:INFO}
quarkus.log.category."com.example".level=${APP_LOG_LEVEL:INFO}
quarkus.log.console.json.enabled=${LOG_JSON:false}
%prod.quarkus.log.console.json.enabled=true
quarkus.http.access-log.enabled=${HTTP_ACCESS_LOG_ENABLED:false}
%prod.quarkus.http.access-log.enabled=${HTTP_ACCESS_LOG_ENABLED:true}
%test.quarkus.http.access-log.enabled=false
